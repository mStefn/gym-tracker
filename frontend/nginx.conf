server {
    listen 8081;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Obsługa bezpośrednia pliku admin.html
    location = /admin.html {
        try_files $uri =404;
    }

    location ~ \.js$ {
        add_header Content-Type application/javascript;
    }
}